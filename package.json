{
  "name": "localink",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "npm start --prefix ./backend",
    "server-install": "cd backend && npm install && npm start",
    "client-install": "cd frontend && npm install && npm start",
    "dev": "concurrently \"npm run server-install\" \"npm run client-install\"",
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix ./frontend npm run build --prefix ./frontend"
  },
  "author": "Olivia and Clara",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.2.0",
    "path": "^0.12.7"
  }
}
